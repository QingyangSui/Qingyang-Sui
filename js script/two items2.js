function requestXML(link){
    var connect = new XMLHttpRequest();
    connect.onreadystatechange = function(){
        if(this.readyState = fucntion(){)
            parseXML(this.responseXML);
        }
    tatus == 200){if(this.readyState ==4 && this.sparseXML(this.responseXML);This passes the XML file we requested to our1llll111111next function
    connect.open("GET", link, true);
    connect.send();
    console.log('requestXML -- 0K');